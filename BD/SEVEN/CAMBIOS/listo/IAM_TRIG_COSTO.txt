SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
ALTER TRIGGER [dbo].[IAM_TRIG_COSTO] ON [dbo].[Kardex] 
FOR INSERT
AS

DECLARE @COD VARCHAR(16),
	@C_REG DATETIME

SET NOCOUNT ON;

SELECT @COD=CODIGO,@C_REG=FECHA_KARDEX FROM INSERTED

EXEC IAM_CALCULAR_COSTO @COD,@C_REG,0
