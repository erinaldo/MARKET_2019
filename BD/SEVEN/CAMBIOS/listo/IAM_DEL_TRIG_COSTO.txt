SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE TRIGGER [dbo].[IAM_DEL_TRIG_COSTO] ON [dbo].[Kardex] 
FOR DELETE
AS

DECLARE @COD VARCHAR(16),
	@C_REG DATETIME

SET NOCOUNT ON;

SELECT @COD=CODIGO,@C_REG=FECHA_KARDEX FROM DELETED

EXEC IAM_CALCULAR_COSTO @COD,@C_REG,0

GO