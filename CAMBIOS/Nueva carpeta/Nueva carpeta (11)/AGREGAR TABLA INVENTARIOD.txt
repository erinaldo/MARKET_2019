SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

SET ANSI_PADDING ON
GO

CREATE TABLE [dbo].[INVENTARIOD](
	[COD_INV] [bigint] NOT NULL,
	[ITEM_INVD] [int] NOT NULL,
	[COD_ART] [char](15) NULL,
	[COSTO_ART] [float] NULL,
	[STOCK_INVD] [float] NULL,
	[CANT_INVD] [float] NULL,
 CONSTRAINT [PK_INVENTARIOD] PRIMARY KEY CLUSTERED 
(
	[COD_INV] ASC,
	[ITEM_INVD] ASC
)WITH (PAD_INDEX  = OFF, STATISTICS_NORECOMPUTE  = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS  = ON, ALLOW_PAGE_LOCKS  = ON) ON [PRIMARY]
) ON [PRIMARY]

GO

SET ANSI_PADDING OFF
GO

ALTER TABLE [dbo].[INVENTARIOD]  WITH CHECK ADD  CONSTRAINT [FK_INVENTARIOD_INVENTARIO] FOREIGN KEY([COD_INV])
REFERENCES [dbo].[INVENTARIO] ([COD_INV])
ON UPDATE CASCADE
ON DELETE CASCADE
GO

ALTER TABLE [dbo].[INVENTARIOD] CHECK CONSTRAINT [FK_INVENTARIOD_INVENTARIO]
GO


